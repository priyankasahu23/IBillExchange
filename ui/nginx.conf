; server {
;     location / {
;         # Enable CORS headers
;         add_header 'Access-Control-Allow-Origin' 'http://localhost:4200';
;         add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS';
;         add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, X-Requested-With';
;         add_header 'Access-Control-Allow-Credentials' 'true';  # if you're using credentials like cookies
;
;         # Handle pre-flight OPTIONS requests
;         if ($request_method = 'OPTIONS') {
;             add_header 'Access-Control-Allow-Origin' 'http://localhost:4200';
;             add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS';
;             add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, X-Requested-With';
;             add_header 'Access-Control-Max-Age' 1728000; # Cache pre-flight response for 20 days
;             return 204;  # No Content response for OPTIONS
;         }
;     }
; }
